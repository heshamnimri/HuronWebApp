version: '3'

services: 
  
  server: 
    build: ./server
    volumes: 
      - ./server:/usr/src/app 
    ports: 
      - 2000:2000

  client: 
    build: ./client
    volumes: 
      - ./client:/usr/src/app 
    ports: 
      - 3000:3000
    depends_on: 
      - server
